apiVersion: v1
kind: Service
metadata:
  name: satisfactory
  labels:
    app: satisfactory
spec:
  type: NodePort
  ports:
    - name: api
      protocol: TCP
      port: 7777
      targetPort: api
      nodePort: 32222
    - name: game
      protocol: UDP
      port: 7777
      targetPort: game
      nodePort: 32222
  selector:
    app: satisfactory
