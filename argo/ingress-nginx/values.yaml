ingress-nginx:
  controller:
    replicaCount: 2
    minAvailable: 1
    metrics:
      enabled: true
      serviceMonitor:
        enabled: true
       #additionalLabels:
       #   release: prometheus-operator